#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../selenium-java-robot/bin/selenium-java-robot" "$@"
  ret=$?
else 
  node  "$basedir/../selenium-java-robot/bin/selenium-java-robot" "$@"
  ret=$?
fi
exit $ret
